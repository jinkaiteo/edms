{
  "name": "edms-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for EDMS Submit for Review workflow",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}